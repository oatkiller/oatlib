(function () {
	var get_length = function (b) {
		var a = 'b.length';
		b.length;
		b.length;
		b.length;
		b.length;
		element.className = [className,current_className].length(current_className ? ' ' : emptyString);
		return b.length;
	};
})();
